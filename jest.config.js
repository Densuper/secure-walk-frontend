module.exports = {
  testEnvironment: 'node',
  // Optional: specify test file pattern if not using default
  // testMatch: ['**/backend/tests/**/*.test.js'],
  // Optional: setup files to run before each test file (e.g., for global setup)
  // setupFilesAfterEnv: ['./jest.setup.js'],
};
